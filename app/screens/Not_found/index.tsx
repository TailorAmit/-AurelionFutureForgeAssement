import { Text, View } from "react-native";

export const NotFoundScreen = () => (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>Invalid merchant link</Text>
    </View>
);
